# Discord
discord.py>=2.3.2

# DB
aiosqlite>=0.19.0

# 환경 변수
python-dotenv>=1.0.0

# Gemini API
google-genai>=1.0.0

# HTTP (비트코인 데이터, 환율, 날씨, 뉴스)
aiohttp>=3.9.0

# epub 텍스트 추출 (도서 학습)
ebooklib>=0.18

# 벡터 검색
chromadb>=0.5.0

# QR 코드 (Lightning 인보이스)
qrcode[pil]>=7.0

# 영상 썸네일 추출 (시스템에 ffmpeg 필요: apt install ffmpeg)
# pip 패키지 불필요, subprocess로 호출
